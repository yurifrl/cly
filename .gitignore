# Yuri Things
.task
.serena

# Local config (not committed)
config.local.yaml
config.local.yml

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Build output
cly
dist/
build/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.worktrees

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.log

# config
*.local
.local-version
.ant-colony/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
graphify-out/cache/

# Reference checkouts (kept locally, untracked)
.references/

# Swift notifier build artifacts (the .tar.gz under pkg/notify/assets/ is
# tracked as a placeholder; real builds overwrite it locally).
pkg/notify/swift/.build/

# Resolved env file (generated by `task envs:op` from .env.op)
.env
